import React from "react";

const Notification = () => {
  return (
    <div className="h-8 bg-red-700 text-white px-4 flex items-center justify-center text-sm text-center md:text-base cursor-pointer">
      free delivery for all offer over $50. Order your Food now!
    </div>
  );
};

export default Notification;
